
export default {
  command: "swagger",
  describe: "Generate swagger",
  handler: async () => {
    // const files = getAppCollections();
    // const files1 = files.slice(0, 5);
    // const files2 = files.slice(5, 13);
    // const files3 = files.slice(13);
    // const postman = [files1, files2, files3].map((fs, i) => new Postman(fs).generate(`name ${i}`));
    // const col = new Postman().mergeFiles("some outer collection", postman);
    //
    // // const swagger = new Swagger(file).generate();
    //
    // console.log(JSON.stringify(col));
    // console.dir(swagger, { depth: null });
  }
};
